import { CustomerMetricsNewState } from "./types";

const initialCustomerMetricsNewState: CustomerMetricsNewState = {
  newPieChartYAxisVariable: "overview",
  newLineChartYAxisVariable: "total",
  newBarChartYAxisVariable: "total",
};

export { initialCustomerMetricsNewState };
