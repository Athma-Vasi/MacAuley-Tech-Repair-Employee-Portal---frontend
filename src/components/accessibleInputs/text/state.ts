import { AccessibleTextInputState } from "./types";

const initialAccessibleTextInputState: AccessibleTextInputState = {
  popoverOpened: false,
  valueBuffer: "",
  isInputFocused: false,
};

export { initialAccessibleTextInputState };
